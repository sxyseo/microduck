# 解读 178 · 八型号横向对比总表

> **解读对象**:`bam/bam/params/` 全部 8 份 m6.json + 各执行器类(`dynamixel/feetech/waveshare/erob/actuator.py`)
> **需要的前置**:[解读 80](80-BAM参数逐字段.md)(字段是什么)· [解读 73](73-换舵机决策树.md)(四道门)

前八篇案例(169–176)逐个读,这篇把它们摆上同一张桌子:8 颗真实上台架辨识过的舵机,
3 个数量级的性能跨度,全在一个目录里。

## 1. 大表:BAM 辨识值(逐份 json 核对,保留三位)

| 目录(json 键名) | 台架供电 vin | kt (N·m/A) | R (Ω) | armature | viscous | 摩擦另注 |
|---|---|---|---|---|---|---|
| xl330 (xl330) | 7.5 V | 0.346 | 2.50 | 0.0016 | 0.0058 | 有 `command_delay`=0.0102 |
| xl320 (xl320) | 7.5 V | 1.009 | **31.59** | 0.0012 | 0.0072 | R 高一个量级 |
| feetech_sts3215_7_4V (sts3215) | 7.4 V | 1.275 | 2.75 | 0.0216 | 0.0282 | `error_gain_ratio`+`max_velocity`=5.096 |
| waveshare_st3025 (同名) | 12.0 V | 1.651 | 3.10 | 0.0048 | 0.0222 | `error_gain_ratio`=1.253 |
| mx64 (mx64) | 15.0 V | 1.602 | 2.32 | 0.0123 | 0.0253 | 无 `q_offset`/`command_delay` |
| mx106 (mx106) | 15.0 V | 2.210 | 2.03 | 0.0260 | 0.0507 | 同上 |
| erob80_50 (同名) | 电流控制 | 4.897 | 1.07 | 0.362 | 1.403 | `friction_base`=3.26 |
| erob80_100 (同名) | 电流控制 | **9.396** | **(json 无此字段)** | **1.135** | **9.709** | `friction_stribeck`=5.23 |

vin 列不是舵机额定电压,是**辨识那次台架的供电**(`VoltageControlledActuator` 类参数;erob 是
电流控制,`max_amps=12`/`max_volts=48`,80_100 档 `damping=2.0`、80_50 档 1.0)。

## 2. 厂商规格对照(只填有出处的,其余待验证)

| 型号 | 堵转力矩 | 电压窗口 | 重量 | 协议 |
|---|---|---|---|---|
| XL330-M288-T | 0.52 N·m @5 V | 3.7–6.0 V | 18 g | Dynamixel V2,TTL |
| STS3215(7.4V) | 1.86 N·m(选型文档口径) | 7.4 V 级(窗口待验证) | ~55–60 g | 飞特 STS 半双工 |
| MX-64 | 6.0 N·m @12 V | 10.0–14.8 V | 126 g | Dynamixel V2,TTL/RS-485 |
| MX-106 | 8.4 N·m @12 V | 10.0–14.8 V | 246 g | Dynamixel V2,TTL/RS-485 |
| XL320 / ST3025 / eRob80 | 待验证 | 台架 7.5 / 12 / ≤48 V | 待验证 | XL320:Dynamixel TTL;ST3025:串行(与 STS3215 同族控制律) |

## 3. 怎么读这张表

- **先看跨度,再看单值**。kt 从 0.346 到 9.396 跨 27 倍;armature 从 0.0012 到 1.135 跨近千倍;
  viscous 从 0.0058 到 9.709 跨三个数量级。同一套训练代码,喂不同的行,练出的是完全不同的机器。
- **kt 高 ≠ 输出劲大**。XL330 的 kt 最小(0.346),但减速比 288.4:1 把它放大成 0.52 N·m;
  kt 是电机端的量,输出端还要乘减速比——表里没有减速比列,拿 kt 直接排名次会排错。
- **摩擦预算是"身份指纹"**。玩具级(xl330,friction_base≈0.012)与工业级(erob80_50,3.26)
  差 270 倍;erob80_100 的 viscous 9.71 意味着每 rad/s 白吃近 10 N·m——这台"鸭"根本不存在,
  它是 BAM 库里给大机器准备的参照物。
- **字段集本身是信息**(80 课结论):`error_gain_ratio` 只出现在飞特系固件的行;
  `command_delay` 有无、长短(0.0102 vs 0.0050)是各自台架实测,不可跨台架比较。
- **空格与缺失别脑补**:erob80_100 的 R 不在 json 里,加载时回落到 `ErobActuator` 类初始值
  (初始 2.0;是否辨识时被锁参,待验证)。

## 你带走的收获

- 一张表看懂"换舵机 = 换动力学":kt 跨 27 倍、armature 跨近千倍,官方 ONNX 作废的数值证据。
- vin 列是台架供电不是额定电压;厂商规格与辨识参数是两张表,混用就是 52 课批过的口径错位。
- 选型回扣四道门:电压→力矩→尺寸→协议;XL330 在"轻"端无人平替,sts3215 在"便宜"端但重 3 倍。
- json 字段缺失 ≠ 数据丢失,缺失回落到执行器类初始值——读表先读脚注。

## 延伸

- [解读 73](73-换舵机决策树.md)/[解读 74](74-选型六维.md):四道门与六维
- [解读 80](80-BAM参数逐字段.md):18 个字段逐个是什么
- `bam/bam/params/*/m6.json` · `bam/bam/actuators.py`(注册表 10 键,参数库只有 8 目录)
- `microduck-replica/docs/执行器选型.md`(XL330/STS3215 厂商规格出处)
